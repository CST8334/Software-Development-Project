import React from 'react'
import { useLocation } from 'react-router';
import styled from 'styled-components';

function WhatsNew() {
    const { pathname } = useLocation();
    return (

    )
}

const StyleWhatsNew = styled.whatsNew`
    

export default whatsNew